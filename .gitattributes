# Normalise line endings so Windows and the Linux CI runner agree.
* text=auto eol=lf

# Windows-only helper scripts keep CRLF.
*.ps1 text eol=crlf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
